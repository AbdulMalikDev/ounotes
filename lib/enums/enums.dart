//*Path Enum for search bar to figure out where user came from to show appropriate results
enum Path {
  Appbar,
  Dialog,
}

enum Document {
  Notes,
  QuestionPapers,
  Syllabus,
  Links,
  Drawer,
  UploadLog,
  Report,
  None,
}

enum Menu {
  Report,
  Pin,
}
